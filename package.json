{
  "name": "pathfinder",
  "version": "1.0.0",
  "description": "A library for finding paths through mazes",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tslint src && tsc",
    "example": "node example",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^4.3.1",
    "typescript": "^2.0.10"
  }
}
