{
  "name": "mauro",
  "version": "1.0.0",
  "description": "A library for finding paths through 2d mazes",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run lint && npm run compile",
    "compile": "tsc",
    "example": "npm run compile && node example",
    "lint": "tslint --project ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "devDependencies": {
    "@cleavera/config": "^1.1.0",
    "tslint": "^5.17.0",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "@cleavera/utils": "^3.4.0"
  }
}
